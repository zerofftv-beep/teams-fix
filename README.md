# SBW Teams (Server Compatible v1.0.1)

## Исправления:
- Мод теперь работает на сервере (убран клиентский код из серверной части)
- Mixin исправлен и не загружает LocalPlayer на сервере

## Команды

### Игроки:
- `/team create <name>`
- `/team invite <player>`
- `/team join <name>`
- `/team leave`

### Операторы:
- `/teamadmin maxsize <number>`
- `/teamadmin nametagdistance <blocks>`
```